// Women's Line is controlled via Admin → App Settings → Women's Line toggle.
// Live value lives in the `app_settings` table (key: 'womens_enabled').
// Use `useWomensEnabled()` from hooks/useAppSettings.ts — not this constant.
